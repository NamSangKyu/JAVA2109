인터페이스(Interface)
	- 부모 자식 관계를 제외한 관계를 만들때 사용
	- 클래스가 상속 받을 수 있는 클래스는 1개
	  부모클래스 외적인 관계를 만들기 위해 인터페이스를 사용
	- 인터페이스는 클래스 대용품으로 사용함
	- 인터페이스는 클래스가 아니기 때문에 직접적으로 생성되지 않음
	  추상화 처리 및 직접적으로 메모리가 할당되지 않는 것으로 구성
	  (static final, 추상메서드)
	- 인터페이스는 클래스가 아니기때문에 클래스가 인터페이스를 여러개 받을 수 있음